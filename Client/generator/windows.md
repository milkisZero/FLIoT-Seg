# Windows 환경에서 PyInstaller 설정

## 1. Python 3.7.9 설치

## 2. PyInstaller 및 요구 사항 설치
```cmd
cd Client
pip install -r requirements.txt
pip install pyinstaller
```

## 3. PyInstaller 실행
`pyinstaller.command` 파일을 사용하여 실행